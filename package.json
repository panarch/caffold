{
  "name": "caffold",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:codex-protocol": "node --test tests/codex-protocol.test.mjs",
    "test:routes": "node --test tests/navigation-routes.test.mjs",
    "test:watch": "node --test tests/watch.test.mjs",
    "test:e2e": "playwright test",
    "test:codex-live": "playwright test --config=playwright.live.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
