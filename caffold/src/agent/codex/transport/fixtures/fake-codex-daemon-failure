#!/bin/sh

if [ "$1 $2 $3" != "app-server daemon restart" ] &&
   [ "$1 $2 $3" != "app-server daemon update" ]; then
  echo "unexpected arguments: $*" >&2
  exit 2
fi

echo "daemon is busy" >&2
exit 9
