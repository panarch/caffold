#!/bin/sh

if [ "$1" = "--version" ]; then
  echo "codex-cli 0.147.0"
  exit 0
fi

if [ "$1 $2 $3" = "app-server daemon --help" ] ||
   [ "$1 $2 $3" = "app-server proxy --help" ]; then
  exit 0
fi

echo "fake Codex does not start an app-server" >&2
exit 1
